'use client';import{useEffect,useState}from'react';
export default function InfrastructureCenter(){const[d,setD]=useState<any>({nodes:[],dbHosts:[],migrations:[]});
 useEffect(()=>{fetch('/api/admin/infrastructure').then(r=>r.json()).then(x=>{if(!x.error)setD(x)})},[]);
 return <div className="stack"><div className="pageHeader"><div><div className="eyebrow">INFRASTRUCTURE</div><h1>Infrastructure Center</h1><p className="muted">Nodes, database hosts and migration readiness in one place.</p></div></div>
 <div className="grid3"><div className="card"><div className="cardTitle">Nodes</div><h2>{d.nodes?.length||0}</h2><p className="muted">Registered compute nodes</p></div><div className="card"><div className="cardTitle">Database Hosts</div><h2>{d.dbHosts?.length||0}</h2><p className="muted">Dedicated DB host registry</p></div><div className="card"><div className="cardTitle">Migrations</div><h2>{d.migrations?.length||0}</h2><p className="muted">Recent migration jobs</p></div></div>
 <div className="card"><div className="cardTitle">Node capacity</div>{d.nodes?.map((n:any)=><div key={n.id} className="listRow"><div><b>{n.name}</b><div className="muted">{n.location}</div></div><div className="muted">{n.capacity_cpu} CPU · {Math.round(n.capacity_memory_mb/1024)} GB RAM · {Math.round(n.capacity_disk_mb/1024)} GB disk</div></div>)}</div>
 <div className="card"><div className="cardTitle">v0.12 migration engine</div><p className="muted">Migration job tracking and target-node validation are installed. Destructive cross-node data transfer stays gated until remote transport/TLS is configured.</p></div></div>}