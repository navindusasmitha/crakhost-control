'use client';
import { Search, Menu, Zap, LogOut } from 'lucide-react';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';
type U={name:string;role:string;credits:string|number};
export default function Header(){const [u,setU]=useState<U|null>(null);const r=useRouter();useEffect(()=>{fetch('/api/auth/me').then(x=>x.ok?x.json():null).then(d=>d?.user&&setU(d.user)).catch(()=>{})},[]);async function logout(){await fetch('/api/auth/logout',{method:'POST'});r.push('/login');r.refresh()}const initials=(u?.name||'CH').split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase();return <><div className="mobileTop"><div className="brand" style={{padding:0,fontSize:16}}><div className="brandMark" style={{width:30,height:30}}><Zap size={16}/></div>CrakHost</div><Menu size={20}/></div><header className="header"><div className="crumbs"><span>CrakHost Cloud</span><span>/</span><span className="crumbActive">Control Panel</span></div><div className="headerRight"><div className="search"><Search size={15}/><input placeholder="Search (Cmd+K)"/></div><div className="profile"><div className="profileText"><b>{u?.name||'Loading account...'}</b><div className="credits">{u?`LKR ${Number(u.credits).toLocaleString()} · ${u.role}`:'Secure session'}</div></div><div className="avatar">{initials}</div><button className="iconBtn" onClick={logout} title="Sign out"><LogOut size={15}/></button></div></div></header></>}
