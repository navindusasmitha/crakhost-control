'use client';import{useState}from'react';export default function DeploymentCenter(){const[u,setU]=useState<any>(null);async function check(){setU(await (await fetch('/api/admin/update/check')).json())}return <div className="stack"><div className="pageHeader"><div><div className="eyebrow">PRODUCTION</div><h1>Deployment & Updates</h1><p className="muted">Domain, HTTPS and GitHub release management.</p></div></div><div className="grid3"><div className="card"><div className="cardTitle">Installed</div><h2>v0.13</h2></div><div className="card"><div className="cardTitle">HTTPS</div><h2>Caddy</h2><p className="muted">Automatic certificate flow on VPS.</p></div><div className="card"><div className="cardTitle">Game catalog</div><h2>Minecraft + FiveM</h2></div></div><div className="card"><div className="cardTitle">Update Center</div><p className="muted">Check the configured GitHub repository for the latest release. Installation is delegated to the root-owned updater instead of exposing Docker/root to the web process.</p><button className="primaryBtn" onClick={check}>Check for updates</button><pre className="console">{JSON.stringify(u||{},null,2)}</pre></div><div className="card"><div className="cardTitle">One-command VPS install</div><code>curl -fsSL https://raw.githubusercontent.com/OWNER/REPO/main/install.sh | sudo bash</code></div></div>}