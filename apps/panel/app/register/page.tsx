'use client';
import Link from 'next/link';import {Zap,UserPlus,Loader2} from 'lucide-react';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Register(){const r=useRouter();const [busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password')})});const text=await res.text();let d:any={};try{d=text?JSON.parse(text):{}}catch{throw new Error(`Registration returned HTTP ${res.status}`)}if(!res.ok)throw new Error(d.error||'Registration failed');let next='/dashboard';if(typeof window!=='undefined'){const incoming=new URLSearchParams(window.location.search);if(incoming.get('plan'))next=`/checkout?${incoming.toString()}`}r.push(next);r.refresh()}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <div className="authWrap"><form className="authBox" onSubmit={submit}><div className="brand authBrand"><div className="brandMark"><Zap size={18}/></div>CrakHost</div><div className="authTitle">Create account</div><div className="authSub">Your customer panel is created automatically and your selected configuration is preserved.</div>{error&&<div className="notice error">{error}</div>}<div className="field"><label>Full name</label><input name="name" placeholder="Your name" required/></div><div className="field"><label>Email address</label><input name="email" type="email" placeholder="you@example.com" required/></div><div className="field"><label>Password</label><input name="password" type="password" minLength={10} placeholder="Minimum 10 characters" required/></div><button className="btn authBtn" disabled={busy}>{busy?<Loader2 size={15} className="spin"/>:<UserPlus size={15}/>} {busy?'Creating...':'Create account'}</button><div className="authFoot">Already registered? <Link href="/login">Sign in</Link></div></form></div>}
