import{Terminal,FolderOpen,Database,HardDrive,Users,CalendarClock,ShieldCheck,Activity,ArrowRight}from'lucide-react';import Link from'next/link';import MarketingNav from '@/components/MarketingNav';
const items=[[Terminal,'Live Console','View runtime output and send commands supported by the managed server.'],[FolderOpen,'File Manager','Manage persistent service files through the server workspace.'],[Database,'Databases','Create and manage database credentials associated with a hosted server.'],[HardDrive,'Backups','Create and track backup workflows for managed services.'],[Users,'Subusers','Server owners and administrators can grant scoped access to other registered users.'],[CalendarClock,'Schedules','Create supported scheduled server tasks and lifecycle actions.'],[ShieldCheck,'Managed Isolation','CrakHost-managed workloads use configured CPU, memory and storage limits.'],[Activity,'Live Resources','Runtime status, CPU and memory values are read from CrakNode.']];
export default function Features(){return <main className="publicRoot"><MarketingNav/>
<section className="publicHero"><div className="publicHeroCopy"><div className="publicEyebrow"><Activity size={11}/>CONTROL PLANE FEATURES</div><h1>One workspace.<br/><span>Connected to the actual service.</span></h1><p>CrakHost combines server runtime controls, persistent data, billing and delegated access around the managed service rather than demo-only dashboards.</p><div className="publicHeroActions"><Link href="/games" className="publicBtn primary">Explore hosting <ArrowRight size={13}/></Link><Link href="/login" className="publicBtn">Open client panel</Link></div></div><aside className="publicHeroPanel"><div className="publicHeroPanelHead"><span>PLATFORM AREAS</span><span className="publicLive">CONNECTED</span></div><div className="publicStatList"><div className="publicStatRow"><span>Runtime layer</span><strong>CrakNode</strong></div><div className="publicStatRow"><span>Billing & service data</span><strong>Database backed</strong></div><div className="publicStatRow"><span>Access control</span><strong>Scoped</strong></div></div></aside></section>
<section className="publicSection alt"><div className="publicSectionHead"><div><div className="publicEyebrow">CUSTOMER WORKSPACE</div><h2>Tools attached to your hosted service.</h2><p>Availability of individual runtime operations depends on the server template and connected node capabilities.</p></div></div><div className="publicFeatureGrid">{items.map(([I,t,d]:any)=><article className="publicFeatureCard" key={t}><div className="publicFeatureIcon"><I size={20}/></div><h3>{t}</h3><p>{d}</p></article>)}</div></section>
</main>}
