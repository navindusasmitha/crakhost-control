import Link from'next/link';import{Server,Cpu,HardDrive,Network,ShieldCheck,ArrowRight,TriangleAlert}from'lucide-react';import MarketingNav from '@/components/MarketingNav';
export default function Vps(){return <main className="publicRoot"><MarketingNav/>
<section className="publicHero"><div className="publicHeroCopy"><div className="publicEyebrow"><Server size={11}/>CLOUD VPS</div><h1>Virtual servers.<br/><span>Only when real capacity is connected.</span></h1><p>CrakHost has a dedicated VPS product area, but checkout remains unavailable until a supported virtualization provider is connected to the control plane.</p><div className="publicHeroActions"><Link href="/support" className="publicBtn primary">Contact support <ArrowRight size={13}/></Link><Link href="/features" className="publicBtn">View platform</Link></div></div><aside className="publicHeroPanel"><div className="publicHeroPanelHead"><span>VPS AVAILABILITY</span><span style={{color:'#fcd34d',fontSize:9,fontWeight:900}}>BACKEND REQUIRED</span></div><div className="publicStatList"><div className="publicStatRow"><span>Public VPS inventory</span><strong>Not published</strong></div><div className="publicStatRow"><span>Hypervisor integration</span><strong>Required</strong></div><div className="publicStatRow"><span>Fake capacity</span><strong>Not shown</strong></div></div></aside></section>
<section className="publicSection"><div className="publicSectionHead"><div><div className="publicEyebrow">PLANNED CAPABILITIES</div><h2>The storefront is ready for a real virtualization provider.</h2><p>These are platform areas, not claims of currently purchasable VPS inventory.</p></div></div><div className="publicFeatureGrid"><article className="publicFeatureCard"><div className="publicFeatureIcon"><Cpu size={20}/></div><h3>Compute plans</h3><p>CPU and memory products can be published after hypervisor capacity is connected.</p></article><article className="publicFeatureCard"><div className="publicFeatureIcon"><HardDrive size={20}/></div><h3>Persistent storage</h3><p>VM disk lifecycle requires a real virtualization-backed storage provider.</p></article><article className="publicFeatureCard"><div className="publicFeatureIcon"><Network size={20}/></div><h3>Networking</h3><p>Public allocations can be managed once the VM backend exposes them to CrakHost.</p></article><article className="publicFeatureCard"><div className="publicFeatureIcon"><ShieldCheck size={20}/></div><h3>Customer ownership</h3><p>Billing and service ownership can use the same CrakHost account system.</p></article></div></section>
<section className="publicSection alt"><div className="publicNotice" style={{display:'flex',gap:13,alignItems:'flex-start'}}><TriangleAlert size={20} style={{color:'#fcd34d',flex:'0 0 auto'}}/><div><b style={{color:'#fff'}}>VPS purchases are intentionally gated.</b><div style={{marginTop:5}}>CrakHost does not invent locations, stock or VM capacity. Purchase controls should be enabled only after a real hypervisor integration is configured and tested.</div></div></div></section>
</main>}
