import Link from 'next/link';import{Zap,Server,ShieldCheck,Gauge,ArrowRight,Check}from'lucide-react';import{db}from'@/lib/db';
export const dynamic='force-dynamic';
export default async function Home(){const{rows:plans}=await db.query(`select slug,name,description,memory_mb,cpu_limit,disk_mb,price_monthly,currency,featured from plans where enabled=true order by sort_order,price_monthly`);return <main style={{minHeight:'100vh',background:'#050506',color:'#fff',fontFamily:'Inter,system-ui,sans-serif'}}><header style={{height:72,display:'flex',alignItems:'center',justifyContent:'space-between',maxWidth:1180,margin:'0 auto',padding:'0 24px',borderBottom:'1px solid #1b1b20'}}><div style={{display:'flex',alignItems:'center',gap:10,fontSize:20,fontWeight:800}}><span style={{display:'grid',placeItems:'center',width:36,height:36,borderRadius:10,background:'#625df5'}}><Zap size={18}/></span>CrakHost</div><nav style={{display:'flex',gap:12}}><Link className="btn" href="/login">Sign in</Link><Link className="btn indigo" href="/register">Get started</Link></nav></header><section style={{maxWidth:1180,margin:'0 auto',padding:'86px 24px 48px',textAlign:'center'}}><div style={{fontSize:12,letterSpacing:2,color:'#8f8cff',fontWeight:800}}>CRAKHOST CLOUD</div><h1 style={{fontSize:'clamp(42px,7vw,76px)',lineHeight:1.02,margin:'16px auto',maxWidth:900}}>Game server hosting that provisions itself.</h1><p style={{maxWidth:720,margin:'0 auto',color:'#9b9baa',fontSize:18,lineHeight:1.7}}>Choose a plan, create your account, pay from your CrakHost wallet and receive a real isolated server on a live CrakNode automatically.</p><div style={{display:'flex',justifyContent:'center',gap:12,marginTop:28,flexWrap:'wrap'}}><Link className="btn indigo" href="#plans">View plans <ArrowRight size={15}/></Link><Link className="btn" href="/login">Customer panel</Link></div></section><section style={{maxWidth:1180,margin:'0 auto',padding:'24px',display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:14}}>{[[Server,'Real Docker Servers','Minecraft workloads are created on your VPS, not simulated.'],[Gauge,'Live Resource Data','CPU, memory, uptime and logs come from CrakNode/Docker.'],[ShieldCheck,'Customer Isolation','Each account sees only its own servers and billing data.']].map(([I,t,d]:any)=><div className="card" key={t}><I size={20}/><h3>{t}</h3><p className="small">{d}</p></div>)}</section><section id="plans" style={{maxWidth:1180,margin:'0 auto',padding:'70px 24px 100px'}}><div style={{textAlign:'center',marginBottom:30}}><div style={{fontSize:12,letterSpacing:2,color:'#8f8cff',fontWeight:800}}>PLANS</div><h2 style={{fontSize:36,margin:'8px 0'}}>Choose your server</h2></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:16}}>{plans.map((p:any)=><div className="card" key={p.slug} style={{position:'relative',outline:p.featured?'1px solid #625df5':'none'}}>{p.featured&&<span className="badge" style={{position:'absolute',right:16,top:16}}>POPULAR</span>}<h3 style={{fontSize:22}}>{p.name}</h3><p className="small">{p.description}</p><div style={{fontSize:32,fontWeight:900,margin:'22px 0'}}>{p.currency} {Number(p.price_monthly).toLocaleString()}<span className="small"> / mo</span></div>{[[`${Math.round(p.memory_mb/1024)} GB RAM`],[`${p.cpu_limit} vCPU`],[`${Math.round(p.disk_mb/1024)} GB disk`]].map(([x])=><div key={x} style={{display:'flex',gap:8,alignItems:'center',margin:'10px 0'}}><Check size={14}/>{x}</div>)}<Link className="btn indigo" style={{width:'100%',justifyContent:'center',marginTop:18}} href={`/register?plan=${encodeURIComponent(p.slug)}`}>Choose {p.name}</Link></div>)}</div></section></main>}
