import Link from 'next/link';import{Headphones,MessageCircle,ShieldCheck,Clock3,ArrowRight}from'lucide-react';import SupportCenter from '../../../components/SupportCenter';import MarketingNav from '@/components/MarketingNav';import{getCurrentUser}from'@/lib/auth';
export default async function Support(){const user=await getCurrentUser();if(user)return <SupportCenter/>;return <main className="publicRoot"><MarketingNav/>
<section className="publicHero"><div className="publicHeroCopy"><div className="publicEyebrow"><Headphones size={11}/>CRAKHOST SUPPORT</div><h1>Need help?<br/><span>Use account-linked support.</span></h1><p>Signed-in customers can open tickets, continue conversations and follow ticket state from the same account that owns their hosting services.</p><div className="publicHeroActions"><Link href="/login" className="publicBtn primary">Open client support <ArrowRight size={13}/></Link><Link href="/features" className="publicBtn">View platform</Link></div></div><aside className="publicHeroPanel"><div className="publicHeroPanelHead"><span>SUPPORT WORKFLOW</span><span className="publicLive">ACCOUNT LINKED</span></div><div className="publicStatList"><div className="publicStatRow"><span>Create tickets</span><strong>Signed-in users</strong></div><div className="publicStatRow"><span>Conversation history</span><strong>Stored</strong></div><div className="publicStatRow"><span>Ticket status</span><strong>Trackable</strong></div></div></aside></section>
<section className="publicSection alt"><div className="publicSectionHead"><div><div className="publicEyebrow">CUSTOMER CARE</div><h2>Support that stays with the service account.</h2><p>No fake contact queue or generated ticket statistics are shown on the public page.</p></div></div><div className="publicFeatureGrid"><article className="publicFeatureCard"><div className="publicFeatureIcon"><MessageCircle size={20}/></div><h3>Ticket conversations</h3><p>Open a request and continue the thread with support staff.</p></article><article className="publicFeatureCard"><div className="publicFeatureIcon"><ShieldCheck size={20}/></div><h3>Account-aware help</h3><p>Requests remain associated with the authenticated customer account.</p></article><article className="publicFeatureCard"><div className="publicFeatureIcon"><Clock3 size={20}/></div><h3>Status tracking</h3><p>Follow open and closed ticket state inside the CrakHost workspace.</p></article></div></section>
<section className="publicSection"><div className="publicNotice" style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:18,flexWrap:'wrap'}}><div><b style={{color:'#fff'}}>Already hosting with CrakHost?</b><div style={{marginTop:5}}>Sign in and open your dedicated support workspace.</div></div><Link href="/login" className="publicBtn primary">Sign in <ArrowRight size={13}/></Link></div></section>
</main>}
