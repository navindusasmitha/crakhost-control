import Link from 'next/link';
import {Plus,Bell,Cpu,MemoryStick,HardDrive,Activity,ArrowUpRight} from 'lucide-react';
import {db} from '@/lib/db';import {nodeFetchFor} from '@/lib/node';import {getCurrentUser,isStaff} from '@/lib/auth';
export const dynamic='force-dynamic';export const revalidate=0;
export default async function Dashboard(){const user=await getCurrentUser();if(!user)return null;const staff=isStaff(user);const params:any[]=staff?[]:[user.id];const where=staff?'':'where s.owner_id=$1';const serverQ=await db.query(`select s.id,s.name,s.identifier,s.status,s.memory_mb,s.cpu_limit,s.disk_mb,s.primary_ip,s.primary_port,s.node_id,n.name node_name,n.location node_location,n.base_url,n.api_token from servers s left join nodes n on n.id=s.node_id ${where} order by s.created_at desc limit 50`,params);const nodeQ=staff?await db.query(`select id,name,location,base_url,api_token,enabled,capacity_cpu,capacity_memory_mb,capacity_disk_mb from nodes where enabled=true order by name`):await db.query(`select distinct n.id,n.name,n.location,n.base_url,n.api_token,n.enabled,n.capacity_cpu,n.capacity_memory_mb,n.capacity_disk_mb from nodes n join servers s on s.node_id=n.id where s.owner_id=$1 and n.enabled=true order by n.name`,[user.id]);const revenueQ=staff?await db.query(`select coalesce(sum(amount),0)::numeric amount,coalesce(max(currency),'LKR') currency from invoices where status='PAID' and paid_at>=date_trunc('month',now())`):await db.query(`select coalesce(sum(amount),0)::numeric amount,coalesce(max(currency),'LKR') currency from invoices where user_id=$1 and status='PAID' and paid_at>=date_trunc('month',now())`,[user.id]);const nodes:any[]=[];for(const n of nodeQ.rows){const t=Date.now();try{const d=await nodeFetchFor(n,'/diagnostics');nodes.push({...n,status:'online',latencyMs:Date.now()-t,...d})}catch(e:any){nodes.push({...n,status:'offline',latencyMs:Date.now()-t,error:e?.message||'Node unavailable'})}}const servers=await Promise.all(serverQ.rows.map(async(s:any)=>{try{const live=await nodeFetchFor(s,`/v1/servers/${encodeURIComponent(s.identifier)}/status`);return {...s,status:String(live.status||s.status),live}}catch{return {...s,status:'offline'}}));const running=servers.filter((s:any)=>s.status==='running').length;const cpuAllocated=serverQ.rows.reduce((a:number,s:any)=>a+Number(s.cpu_limit||0),0);const memoryAllocated=serverQ.rows.reduce((a:number,s:any)=>a+Number(s.memory_mb||0),0);const cpuCapacity=nodeQ.rows.reduce((a:number,n:any)=>a+Number(n.capacity_cpu||0),0);const memoryCapacity=nodeQ.rows.reduce((a:number,n:any)=>a+Number(n.capacity_memory_mb||0),0);const revenue=Number(revenueQ.rows[0]?.amount||0),currency=String(revenueQ.rows[0]?.currency||'LKR');return <><div className="pageHead"><div><p>{staff?'CRAKHOST CLOUD':'CUSTOMER PANEL'}</p><h1>{staff?'Infrastructure Overview':'My Hosting'}</h1><p>Live data from your real CrakNode workloads.</p></div><div className="actions">{staff&&<Link className="btn" href="/operations"><Bell size={15}/>Operations</Link>}<Link className="btn indigo" href="/checkout"><Plus size={15}/>Order Server</Link></div></div><div className="grid4"><Metric icon={<Activity size={16}/>} label="Servers" value={String(servers.length)} sub={`${running} running · ${Math.max(0,servers.length-running)} offline/stopped`}/><Metric icon={<Cpu size={16}/>} label="CPU Allocated" value={`${fmt(cpuAllocated)} vCPU`} sub={staff?`${fmt(cpuCapacity)} vCPU node capacity`:'Across your servers'}/><Metric icon={<MemoryStick size={16}/>} label="Memory Allocated" value={`${fmtGB(memoryAllocated)} GB`} sub={staff?`${fmtGB(memoryCapacity)} GB node capacity`:'Across your servers'}/><Metric icon={<HardDrive size={16}/>} label={staff?'Paid This Month':'My Spend This Month'} value={`${currency} ${revenue.toLocaleString()}`} sub="Calculated from paid invoices"/></div><section className="section"><div className="sectionTitle">{staff?'Real Servers':'My Servers'}</div><div className="serverTable">{servers.length===0?<div className="serverRow"><div><div className="serverName">No servers yet</div><div className="serverSub">Choose a plan to provision your first real server.</div></div></div>:servers.map((s:any)=><div className="serverRow" key={s.id}><div><div className="serverName">{s.name}</div><div className="serverSub">{s.node_name||'No node'} · {s.primary_ip}:{s.primary_port}</div></div><div className="hideSm"><div className="status"><span className={s.status==='running'?'pulse':''}/>{s.status.toUpperCase()}</div></div><div className="hideMd"><div className="small">RAM</div>{s.live?.memory?`${Math.round(Number(s.live.memory))} MB / `:''}{fmtGB(Number(s.memory_mb))} GB</div><div className="hideSm"><div className="small">CPU</div>{s.live?.cpu!=null?`${Number(s.live.cpu).toFixed(1)}% live`:`${fmt(Number(s.cpu_limit))} vCPU`}</div><div className="hideMd"><div className="small">DISK</div>{fmtGB(Number(s.disk_mb))} GB</div><Link className="btn" href={`/servers/${s.identifier}`}><ArrowUpRight size={15}/></Link></div>)}</div></section>{staff&&<section className="section"><div className="card"><div className="sectionTitle">Live Nodes</div><div className="list">{nodes.map((n:any)=><div className="listItem" key={n.id}><div><b>{n.name}</b><div className="small">{n.location} · {n.status==='online'?`${n.latencyMs} ms · ${n.runningContainers||0}/${n.managedContainers||0} managed running`:n.error}</div></div><span className="badge">{n.status.toUpperCase()}</span></div>)}</div></div></section>}</>}
function Metric({icon,label,value,sub}:{icon:React.ReactNode,label:string,value:string,sub:string}){return <div className="card"><div className="metricTop"><span>{label}</span>{icon}</div><div className="metricValue">{value}</div><div className="small">{sub}</div></div>}function fmt(v:number){return Number.isFinite(v)?Number(v.toFixed(2)).toString():'0'}function fmtGB(mb:number){return Number.isFinite(mb)?Number((mb/1024).toFixed(1)).toString():'0'}
