import{NextRequest,NextResponse}from'next/server';import{getCurrentUser,isStaff}from'@/lib/auth';import{db}from'@/lib/db';import{audit}from'@/lib/audit';
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!isStaff(u))return NextResponse.json({error:'Forbidden'},{status:403});const{id}=await params;const node=await db.query('select id,name from nodes where id=$1 limit 1',[id]);if(!node.rowCount)return NextResponse.json({error:'Node not found'},{status:404});const b=await req.json().catch(()=>({}));const draining=!!b.draining;const q=await db.query(`select value from system_settings where key='operations' limit 1`);const current=q.rows[0]?.value||{};const set=new Set<string>(Array.isArray(current.drainNodes)?current.drainNodes.map(String):[]);if(draining)set.add(id);else set.delete(id);const value={...current,drainNodes:[...set]};await db.query(`insert into system_settings(key,value,updated_by,updated_at) values('operations',$1::jsonb,$2,now()) on conflict(key) do update set value=excluded.value,updated_by=excluded.updated_by,updated_at=now()`,[JSON.stringify(value),u.id]);await audit(u.id,draining?'node.drain.enable':'node.drain.disable','node',id,{name:node.rows[0].name});return NextResponse.json({ok:true,draining});}
