import{NextResponse}from'next/server';import{getCurrentUser,isStaff}from'@/lib/auth';import{db}from'@/lib/db';
export const dynamic='force-dynamic';
export async function GET(){const u=await getCurrentUser();if(!isStaff(u))return NextResponse.json({error:'Forbidden'},{status:403});
 const [n,h,m,o]=await Promise.all([db.query(`select n.id,n.name,n.location,n.enabled,n.capacity_cpu,n.capacity_memory_mb,n.capacity_disk_mb,n.last_seen_at,n.agent_version,coalesce(sum(s.cpu_limit) filter(where s.status<>'deleted'),0)::numeric used_cpu,coalesce(sum(s.memory_mb) filter(where s.status<>'deleted'),0)::int used_memory_mb,coalesce(sum(s.disk_mb) filter(where s.status<>'deleted'),0)::int used_disk_mb,count(s.id) filter(where s.status<>'deleted')::int server_count from nodes n left join servers s on s.node_id=n.id group by n.id order by n.name`),db.query(`select id,name,engine,host,port,enabled from database_hosts order by name`),db.query(`select sm.id,sm.status,sm.progress,sm.detail,sm.created_at,sm.completed_at,s.name server_name,s.identifier,src.name source_node,tgt.name target_node from server_migrations sm join servers s on s.id=sm.server_id left join nodes src on src.id=sm.source_node_id left join nodes tgt on tgt.id=sm.target_node_id order by sm.created_at desc limit 20`),db.query(`select value from system_settings where key='operations' limit 1`)]);
 const ops=o.rows[0]?.value||{};const drains=new Set<string>(Array.isArray(ops.drainNodes)?ops.drainNodes.map(String):[]);
 const nodes=n.rows.map((x:any)=>({...x,draining:drains.has(String(x.id)),online:!!x.enabled&&!!x.last_seen_at&&(Date.now()-new Date(x.last_seen_at).getTime()<120000),free_cpu:Math.max(0,Number(x.capacity_cpu)-Number(x.used_cpu)),free_memory_mb:Math.max(0,Number(x.capacity_memory_mb)-Number(x.used_memory_mb)),free_disk_mb:Math.max(0,Number(x.capacity_disk_mb)-Number(x.used_disk_mb))}));
 return NextResponse.json({nodes,dbHosts:h.rows,migrations:m.rows,operations:ops,summary:{nodes:nodes.length,online:nodes.filter((x:any)=>x.online).length,draining:nodes.filter((x:any)=>x.draining).length,servers:nodes.reduce((a:number,x:any)=>a+Number(x.server_count||0),0),memoryUsedMb:nodes.reduce((a:number,x:any)=>a+Number(x.used_memory_mb||0),0),memoryTotalMb:nodes.reduce((a:number,x:any)=>a+Number(x.capacity_memory_mb||0),0)}});}
