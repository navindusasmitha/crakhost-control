import {NextResponse}from'next/server';import {requireServer}from'@/lib/server-access';import {nodeFetchForServer}from'@/lib/node';const allowed=new Set(['start','stop','restart','kill']);export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{await requireServer(id,'console');const {action}=await req.json();if(!allowed.has(action))return NextResponse.json({error:'Unsupported action'},{status:400});return NextResponse.json(await nodeFetchForServer(id,`/v1/servers/${encodeURIComponent(id)}/action`,{method:'POST',body:JSON.stringify({action})}))}catch(e:any){return NextResponse.json({error:e.message||'CrakNode unavailable'},{status:e.message==='UNAUTHENTICATED'?401:e.message==='NOT_FOUND'?404:503})}}
