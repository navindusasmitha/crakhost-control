import {NextResponse}from'next/server';import {requireServer}from'@/lib/server-access';import {nodeFetchForServer}from'@/lib/node';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{await requireServer(id);return NextResponse.json(await nodeFetchForServer(id,`/v1/servers/${encodeURIComponent(id)}/status`))}catch(e:any){if(e.message==='UNAUTHENTICATED')return NextResponse.json({error:'Unauthorized'},{status:401});if(e.message==='NOT_FOUND')return NextResponse.json({error:'Not found'},{status:404});return NextResponse.json({status:'node_offline',cpu:0,memory:0,memoryLimit:8192,uptime:'—',error:e.message},{status:503})}}
