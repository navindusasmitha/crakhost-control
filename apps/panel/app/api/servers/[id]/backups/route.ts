import {NextRequest,NextResponse} from 'next/server';import {requireServer} from '@/lib/server-access';import {nodeFetchForServer} from '@/lib/node';import {db} from '@/lib/db';
export async function GET(_:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const {server}=await requireServer(id,'backups');const {rows}=await db.query('select id,name,status,size_bytes,created_at from backups where server_id=$1 order by created_at desc limit 30',[server.id]);return NextResponse.json({backups:rows})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const {server,user}=await requireServer(id,'backups');const b=await req.json().catch(()=>({}));const name=typeof b.name==='string'&&b.name.trim()?b.name.trim().slice(0,100):`Backup ${new Date().toISOString().slice(0,16).replace('T',' ')}`;const {rows}=await db.query("insert into backups(server_id,name,status,created_by) values($1,$2,'CREATING',$3) returning id",[server.id,name,user.id]);try{const n=await nodeFetchForServer(id,`/v1/servers/${encodeURIComponent(id)}/backup`,{method:'POST',body:JSON.stringify({backupId:rows[0].id})});await db.query("update backups set status='READY',size_bytes=$2,remote_path=$3 where id=$1",[rows[0].id,n.size||0,n.path||'']);}catch(e){await db.query("update backups set status='FAILED' where id=$1",[rows[0].id]);throw e}return NextResponse.json({ok:true,id:rows[0].id},{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:502})}}
